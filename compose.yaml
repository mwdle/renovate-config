name: Renovate
services:
  Renovate:
    image: renovate/renovate:44.104-full@sha256:0729fb6eba240a126aed4c22084c69b11c2ffac1c5f210d531d047fd337a6e53
    container_name: Renovate
    restart: no
    hostname: renovate
    networks:
      Renovate:
    volumes:
      - renovate_cache:/tmp/renovate/cache
      - renovate_repos:/tmp/renovate/repos
    environment:
      RENOVATE_PLATFORM: forgejo
      RENOVATE_ENDPOINT: https://${GIT_SERVER_HOSTNAME}/api/v1
      RENOVATE_TOKEN: ${GIT_SERVER_TOKEN}
      RENOVATE_AUTODISCOVER: true
      RENOVATE_AUTODISCOVER_FILTER: ${GIT_REPO:-${GIT_SERVER_ORG}/*}
      RENOVATE_ONBOARDING_CONFIG: '{"extends": ["local>${GIT_SERVER_ORG}/renovate-config"]}'
      RENOVATE_GITHUB_COM_TOKEN: ${GITHUB_TOKEN}
      RENOVATE_TIMEZONE: ${TZ}

    user: "${UID}:${GID}"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

    logging:
      driver: journald

    deploy:
      resources:
        limits:
          cpus: 1.5
          memory: 2GB
          pids: 50

networks:
  Renovate:
    external: true

volumes:
  renovate_cache:
  renovate_repos:
